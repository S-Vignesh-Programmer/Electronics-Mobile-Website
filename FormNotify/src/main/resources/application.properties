# ===========================
# MongoDB Configuration
# ===========================
spring.data.mongodb.uri=mongodb+srv://vignesh:vignesh2000@electronicsdb.dw8fzes.mongodb.net/electronicsdb?retryWrites=true&w=majority


# ===========================
# JWT Configuration
# ===========================
jwt.secret=${JWT_SECRET:defaultSecretThatIsAtLeast64CharactersLongForHS512Algorithm123456789}
jwt.expiration=${JWT_EXPIRATION:86400000} # milliseconds (1 day)

# ===========================
# CORS Configuration
# ===========================
spring.web.cors.allowed-origins=${CORS_ALLOWED_ORIGINS:http://localhost:5173,http://localhost:3000}
spring.web.cors.allowed-methods=${CORS_ALLOWED_METHODS:GET,POST,PUT,DELETE,OPTIONS}
spring.web.cors.allowed-headers=${CORS_ALLOWED_HEADERS:Authorization,Content-Type,Accept,X-Requested-With,Cache-Control}
spring.web.cors.allow-credentials=${CORS_ALLOW_CREDENTIALS:true}

# ===========================
# Server Settings
# ===========================
server.port=${SERVER_PORT:8080}

# ===========================
# Application Info & Logging
# ===========================
spring.application.name=FormNotify
